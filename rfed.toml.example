# Deprecated placeholder.
#
# rfed no longer reads `rfed.toml`; the live config file is `<config_dir>/config`
# in Reticulum's native config format.
#
# Use `config.txt.example` as the editable sample, or start `rfed` once and let
# it write a commented `config` file into the chosen config directory.
