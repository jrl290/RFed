# Rust build artifacts
/target/
rfed/target/

# Runtime dump written by rfed into its working directory
status.json

# Cargo lock is committed for binary crates
# (uncomment the next line for library crates)
# Cargo.lock

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# Runtime data & debug files
*.txt
*.log
blobs/
identity
subscriptions.rmp
notify_registrations.rmp
deferred_delivery.rmp
peers.rmp

# APNs credentials (never commit)
utilities/apns_bridge.conf
utilities/*.p8
__pycache__/
apns-bridge/apns_bridge
fcm-bridge/fcm_bridge
rfed/rfed
rfed-nas.status

# Local release packaging output
/dist/
/dist-release-set/
/dist-test/

# Local sample config written by rfed when using the repo root as config dir
/config

# Runtime status snapshots (per-node output, not source — rfed.status was
# tracked by accident and froze a May snapshot into the repo)
rfed.status
*.status
run_output.json
